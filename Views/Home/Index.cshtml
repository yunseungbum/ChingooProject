@model Chingoo.ViewModels.HomeViewModel

@{
    ViewData["Title"] = "홈";
}

<div class="home-board-layout">

    @await Html.PartialAsync("Partials/_Sidebar", Model.BoardMenu)

    <section class="board-content" aria-label="게시판 콘텐츠">
        @await Html.PartialAsync("Partials/_NoticeBoard")
    </section>
</div>
